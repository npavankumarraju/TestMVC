name: Deploy to GitHub Pages

on:
  push:
    branches: [ "main" ]

jobs:
  deploy-to-github-pages:
    # use ubuntu-latest image to run steps on
    runs-on: self-hosted
    permissions:
      contents: write
    steps:
      - name: Deploy to IIS
        run: |
          ./run_as.ps1 Y
